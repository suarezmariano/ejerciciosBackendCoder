export default class Superficie {
  static cuadrado(x: number, y: number) {
    return x * y;
  }

  static triangulo(x: number, y: number) {
    return (x * y) / 2;
  }

  static rectangulo(x: number, y: number) {
    return x * y;
  }
}
